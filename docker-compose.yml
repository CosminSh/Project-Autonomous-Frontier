services:
  db:
    image: timescale/timescaledb:latest-pg15
    volumes:
      - postgres_data:/var/lib/postgresql/data
    environment:
      - POSTGRES_DB=terminal_frontier
      - POSTGRES_USER=admin
      - POSTGRES_PASSWORD=password
      - POSTGRES_SHARED_BUFFERS=64MB
      - POSTGRES_WORK_MEM=2MB
      - POSTGRES_MAX_CONNECTIONS=40
    ports:
      - "5432:5432"
    deploy:
      resources:
        limits:
          memory: 350M
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -d terminal_frontier -U admin"]
      interval: 30s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
    deploy:
      resources:
        limits:
          memory: 80M
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 30s
      timeout: 5s
      retries: 5

  backend:
    build: ./backend
    volumes:
      - ./backend:/app
      - ./frontend:/frontend
      - ./docs:/app/docs
    ports:
      - "3000:8000"
      - "8000:8000"
    environment:
      - DATABASE_URL=postgresql://admin:password@db:5432/terminal_frontier
      - REDIS_URL=redis://redis:6379/0
      - ADMIN_KEY=change-me-before-deploy
    deploy:
      resources:
        limits:
          memory: 350M
    depends_on:
      - db
      - redis
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/api/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    labels:
      - "autoheal=true"

  autoheal:
    image: willfarrell/autoheal:latest
    restart: always
    deploy:
      resources:
        limits:
          memory: 50M
    environment:
      - AUTOHEAL_CONTAINER_LABEL=autoheal
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock

volumes:
  postgres_data:

